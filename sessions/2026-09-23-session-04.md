# Session 04 — a public build log, behind a gate

## At a glance

| | |
|---|---|
| **Date** | 23 September 2026 |
| **Active working time** | One block, from checkout at 07:15 UTC to the commit that follows this log |
| **Shipped** | `site/log.html`, generated from `BUILDING_IN_PUBLIC.md`; a privacy gate that every file must pass before `site/` is written; `assets/` copied into `site/assets/` |
| **The day's turn** | The site gets its first page meant to be read by strangers, so the privacy rule became code rather than a checklist |

## Timeline

| Time (UTC) | What |
|---|---|
| 07:15 | Branch `claude/public-build-log-page-vqkkdf` checked out from `main` at `3fc6862`. |
| ~07:16 | Name patterns found by reading the payer column of the workbook and the commit authorship. |
| 07:18 | `privacy_gate.py` written and tested against planted names, emails, phones, references and the two allowlisted strings. |
| ~07:19 | `log.template.html` and the parser in `build_site.py`. Missing-field, missing-marker and bad-label cases all fail the build. |
| 07:20 | First render at 1360 and 390 wide. Two fixes: failure text started lower-case, the intro wrapped to four lines. Re-rendered. |

## What got built

- **`log.template.html`** — mark and name left, one CTA right, a numbered day nav,
  mustard `#C6922A` page border. Hero, the clock view in an iframe, then the log
  newest-first.
- **`build_site.py`** — now stages every output in memory, runs the gate, and only
  then writes. The four existing pages are built exactly as before; their output
  is byte-identical. New: `parse_log`, `build_log`, the assets copy.
- **`privacy_gate.py`** — importable, and runnable alone against `site/`.

## Decisions

### The owner's

- **Stills, not trimmed video, is left to the assistant**, with a requirement to
  say which. Stills were used.
- **Carry the name patterns inside the script**, because `names.local.txt` will
  not exist on Netlify.
- **Allowlist exactly two strings**, the portfolio and Bluesky handles.
- **Ask before touching `robots.txt`.** Left as it is.

### The assistant's

- **Names are stored as salted hashes, not plain text.** Carrying them in the
  script and never committing them are both rules here; hashing satisfies both
  as far as `git grep` goes. Rejected: plain patterns (commits the names).
  Stated plainly in the file: a dictionary of first names reverses these hashes,
  so it is obscurity, not secrecy, and the repository still needs to stay private.
- **Names match inside longer words**, so a handle glued to a surname is caught.
  That is also why the allowlist is needed at all.
- **Binaries are scanned for names and emails only**, in their printable strings
  and PNG text chunks. Phone and reference shapes turn up by chance in random
  bytes. Rejected: skipping binaries (image metadata can carry an author).
- **The big number is derived, not hand-written.** The first figure in the bold
  phrase of "The number:", or its first sentence when there is no bold. Spelled
  numbers become digits. Rejected: a per-day override table in code, which would
  put editorial text somewhere nobody reads it.
- **Unknown field labels fail the build** as well as missing ones, so a typo does
  not silently fold a field into the one above it.
- **No `BUILDING_IN_PUBLIC.md` entry for today.** The log page reads that file,
  so a Day 4 entry would publish Day 4 at launch, which the brief ruled out.

## Roadblocks

- **Playwright was not importable from Python.** Rendered with Node and
  `playwright-core` against the pre-installed Chromium instead. A few minutes.
- **Google Fonts do not load in this container** (the proxy's certificate), so
  the renders use fallback faces. Layout was checked; typography needs the
  deploy preview.

## Prompts and briefs

> Add a public build-log page to this project. Read CLAUDE.md first —
> Netlify is already connected and build_site.py already builds site/.
> EXTEND the existing pipeline; do not replace netlify.toml and do not
> rewrite build_site.py's existing four pages.

The rest of the brief set the page pattern, parsing rules, media, and the privacy
gate. It is held in the pull request description rather than repeated here. It
left open which traveller names count; the assistant took them from the workbook
and the commit authorship.

## Artefacts

- `privacy_gate.py` (new), `log.template.html` (new), `build_site.py` (changed)
- `site/log.html`, `site/assets/*` (generated)
- `CLAUDE.md` (template table, pipeline note, privacy gate)

## Open at end of session

- `robots.txt` still disallows everything, and the Netlify site is still Private.
  The page is public in intent only until both change.
- Day 2's number reads "80,370 / points to 6,830". Bolding a different phrase in
  that entry would change it.
- Guest names on the image-only pages of the stays PDF were not read; if they
  include anyone not already in the gate, add them.
